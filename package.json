{
  "name": "logic-namping",
  "version": "0.0.1",
  "description": "logic-namping",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/sawara7/logic-namping.git"
  },
  "main": "dist/index.js",
  "scripts": {
    "install-rimraf": "npm install --save-dev rimraf",
    "cleanup": "rimraf package-lock.json && rimraf dist && rimraf node_modules",
    "build": "npm run install-rimraf && npm run cleanup && npm install && tsc",
    "tsc": "tsc",
    "test": "jest"
  },
  "author": "sawara",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.14.34",
    "rimraf": "^4.4.1",
    "typescript": "^4.3.5"
  },
  "bundleDependencies": [],
  "dependencies": {
    "utils-general": "github:sawara7/utils-general",
    "utils-trade": "github:sawara7/utils-trade"
  }
}
